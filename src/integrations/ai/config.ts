/**
 * AI service configuration
 * 
 * This file re-exports configuration from the central config.
 * Use the centralized configuration at @/config for all settings.
 */

import { AI_CONFIG, AI_DEFAULTS } from '@/config';

// Re-export the configuration
export { AI_CONFIG, AI_DEFAULTS };
